version: '3.*'

services:
  everest:
    build:
      context: .
      no_cache: true
    container_name: front
    env_file:
      - .env
      - path: .env.local
        required: false
    restart: unless-stopped
    ports:
      - ${PORT}:${PORT}
